import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Revenue Forecast' }

export default function RevenueForecastLayout({ children }: { children: React.ReactNode }) {
  return children
}
