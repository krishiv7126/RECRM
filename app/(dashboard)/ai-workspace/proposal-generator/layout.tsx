import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Proposal Generator' }

export default function ProposalGeneratorLayout({ children }: { children: React.ReactNode }) {
  return children
}
