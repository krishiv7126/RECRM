import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Lead Intelligence' }

export default function LeadIntelligenceLayout({ children }: { children: React.ReactNode }) {
  return children
}
